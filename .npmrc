# required for jest-expo + Metro under apps/mobile — pnpm's default isolated
# node_modules layout breaks Jest's react-native transformIgnorePatterns match
# (symlink resolution) and would hit the same issue in Metro's bundler
node-linker=hoisted
